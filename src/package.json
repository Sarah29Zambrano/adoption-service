{
  "name": "adoption-service",
  "version": "1.0.0",
  "description": "API REST para gestión de adopciones de mascotas con tests funcionales y documentación Swagger",
  "main": "services/index.js",
  "scripts": {
    "start": "node services/index.js",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:ci": "jest --forceExit --ci"
  },
  "dependencies": {
    "express": "^4.18.2",
    "swagger-jsdoc": "^6.2.8",
    "swagger-ui-express": "^5.0.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}